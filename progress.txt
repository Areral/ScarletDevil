# Ralph Progress Log
Started:  4 июн 2026 г. 23:08:39
---

## 2026-06-04 - US-C01: Аудит и настройка параметров L4 TCP-проверки

### Что сделано

1. **Увеличены таймауты TCP-проверки в go_core/main.go:**
   - DNS resolution: 2s → 4s
   - TCP dial (попытка 1): 2.5s → 8s
   - TCP dial (попытка 2): 10s (после 2s задержки)

2. **Добавлен retry-механизм:**
   - 2 попытки на узел
   - Экспоненциальная задержка: 2s между попытками
   - Retry только для timeout (connection_refused не ретраится)
   - Статистика: l4_retry_attempts, l4_retry_recovered

3. **Добавлено логирование причин отказа L4:**
   - dns_error, timeout, connection_refused, invalid_config, other
   - Статистика пишется в data/go_stats_{uid}.json через 3-й аргумент CLI
   - Python Inspector читает L4-статистику после завершения Go-процесса
   - Причины отказов попадают в data/stats_*.json

4. **L4 survival rate в логах:**
   - Go: `✔[ФИЛЬТРАЦИЯ L4]: Отбраковано X, Выжило: Y (Z% survival)`
   - Python: `L4 failure breakdown: {...} | retries=N recovered=M`

### Изменённые файлы

- go_core/main.go — основная логика L4 (checkL4, runL4Phase, main)
- core/engine.py — IPC: передача stats-файла, чтение L4-статистики
- core/exporter.py — включение l4_failure_reasons в stats_*.json
- main.py — передача l4_failure_reasons из Inspector в Exporter
- prd.json — US-C01 помечен как passes: true

### Learnings for future iterations:
- **L4 checking живёт в Go, НЕ в Python.** Python только оркестрирует (вызывает Go binary). Если нужно менять параметры проверки — менять нужно в go_core/main.go.
- **IPC между Python и Go — файлы JSON.** Добавление нового канала данных (L4 stats) потребовало третий CLI-аргумент и новый stats-файл.
- **timeout vs connection_refused:** При timeout есть смысл ретраить (сеть может быть перегружена), при connection_refused — нет (сервер активно отвергает).
- **go vet:** Предупреждает о формате адреса для IPv6 даже если код корректно обрабатывает оба случая. Лучше использовать if/else вместо перезаписи переменной.
- **pydantic v1/v2:** Локально установлен pydantic 1.10.12, но код требует 2.6.1. Python import падает, но compile — OK. CI должен иметь правильную версию.

## 2026-06-04 - US-C02: Аудит и настройка параметров L7 sing-box валидации

### Что сделано

1. **Добавлен детальный L7 failure reason tracking в Go:**
   - Новая структура `L7Stats` с полями: HTTPTimeout, HTTPTLSError, HTTPBadStatus, HTTPOtherError,
     SpeedTimeout, SpeedTLSError, SpeedTooSlow, SpeedOtherError, SingboxCrash, ProtocolMismatch
   - `testHTTPPing` возвращает категорию ошибки: timeout / tls_error / bad_status / http_error
   - `testHTTPSpeed` возвращает категорию ошибки: timeout / tls_error / too_slow / http_error
   - Трекинг отказов в `processSingboxBatch`: HTTP ping failures, speed test failures,
     singbox startup crash, no-valid-outbound (protocol_mismatch)
   - Трекинг в `processSingboxRecursive`: recursive depth exhausted → singbox_crash
   - `runL7Phase` инициализирует L7 stats (Total) и финализирует Survived

2. **Увеличены таймауты L7:**
   - HTTP ping buffer: 1.5s → 10s (total ~15s с maxLatency=5000ms)
   - Speed test non-champion: 8s → 15s
   - Sing-box startup deadline: 5s → 8s

3. **Уменьшен размер батча:**
   - BATCH_SIZE: 100 → 50 (в config/settings.yaml)
   - Комментарий: меньше батчей → точнее контроль, но больше sing-box рестартов

4. **Комбинированные L4+L7 stats в IPC:**
   - Новая функция `writeCombinedStats()` — пишет L4 (верхний уровень) + L7 (вложенный объект "l7")
   - Формат: `{"total": N, "dns_error": N, ..., "l7": {"total": N, "http_timeout": N, ...}}`
   - Печать L7 failure breakdown при завершении

5. **Python получает и логирует L7 stats:**
   - `Inspector._extract_l4_stats()` / `Inspector._extract_l7_stats()` — новые хелперы
   - `Inspector.l7_stats` dict + `l7_total` / `l7_survived` поля
   - L7 breakdown логируется в loguru
   - `Exporter.write()` принимает `l7_stats` параметр и пишет в `stats_*.json`
   - `main.py` передаёт `l7_stats` из Inspector в Exporter

### Изменённые файлы

- go_core/main.go — L7Stats struct, модификации testHTTPPing/testHTTPSpeed, processSingboxBatch,
  processSingboxRecursive, runL7Phase, main, writeCombinedStats
- core/engine.py — Inspector: l7_stats поля, _extract_l4_stats(), _extract_l7_stats()
- core/exporter.py — write() принимает l7_stats, пишет в stats JSON
- main.py — передача l7_stats в Exporter
- config/settings.yaml — BATCH_SIZE: 50
- prd.json — US-C02 passes: true

### Learnings for future iterations:
- **L7 sing-box architecture:** Каждый батч запускает отдельный sing-box процесс с SOCKS-инбаундами.
  Один крашнувшийся outbound может обрушить весь батч → recursive splitting пополам до изоляции проблемного узла.
- **HTTP ping vs speed test:** Два независимых этапа в каждом батче. Ping проверяет connectivity (204 response),
  speed test замеряет пропускную способность (512KB download). Узел должен пройти ОБА этапа.
- **L7 failure categories:** HTTP ping failure происходит через SOCKS-прокси → ошибки могут быть
  timeout (прокси не отвечает), tls_error (TLS handshake failed), bad_status (не 204), http_error (всё остальное).
  Speed test failure добавляет too_slow (недостаточно скорости).
- **Singbox crash ≠ node failure:** Краш sing-box может быть вызван багом sing-box или несовместимой
  конфигурацией, а не проблемой самого узла. Recursive splitting помогает изолировать проблемный узел.
- **Stats channel evolution:** Переход от плоского L4-only stats к вложенному L4+L7 формату.
  Вложенный объект "l7" добавлен без ломки существующего L4 парсинга.
- **Timeout tuning tradeoffs:** Увеличение таймаутов (15s HTTP, 15s speed, 8s startup) улучшает
  detection rate для медленных узлов, но увеличивает общее время проверки. При 50 L4 survivors
  и 50 per batch: ~1 batch → ~35s на батч (8s startup + http checks + speed checks).
---

## 2026-06-04 - US-C03: Балансировка шардов и курирование источников

### Что сделано

1. **Изменено распределение узлов по шардам (main.py):**
   - Было: chunk-based `all_nodes[idx*chunk : (idx+1)*chunk]` — drone 0 получал первые 25% узлов
   - Стало: round-robin `all_nodes[idx::count]` — каждый дрон получает каждый N-й узел
   - Это гарантирует что узлы из каждого источника равномерно распределены по всем дронам
   - Устраняет проблему кластеризации хороших источников в одном шарде

2. **Добавлен класс SourceHealth (core/parser.py):**
   - Персистентное отслеживание здоровья источников
   - Загрузка/сохранение `data/source_health.json`
   - Счётчик последовательных провалов (consecutive_failures)
   - Авто-исключение после >5 провалов на 24 часа
   - Восстановление при появлении живых узлов
   - Каждый дрон пишет свой шард-файл: `data/source_health_{idx}.json`
   - Метод `filter_active()` для фильтрации источников перед fetching

3. **Source yield в телеметрии:**
   - `source_yields` добавлен в `stats_*.json` через `Exporter.save_files()`
   - Для каждого источника: parsed, alive, yield_pct
   - `merge.py` агрегирует yield со всех дронов и логирует top-5 / bottom-3

4. **Персистентность между запусками:**
   - `.gitignore`: добавлено исключение `!data/source_health.json`
   - Workflow: каждый дрон загружает `data/source_health_*.json` как артефакт
   - Merge step: скрипт объединяет health-файлы со всех дронов (по last_check)
   - Объединённый `data/source_health.json` коммитится вместе с sub-файлами

### Изменённые файлы

- `main.py` — round-robin sharding, SourceHealth интеграция, source_yields в Exporter
- `core/parser.py` — SourceHealth class (+140 lines), filter в fetch_and_parse
- `core/exporter.py` — source_yields параметр в save_files и stats JSON
- `merge.py` — агрегация и логирование source_yields (top/bottom источников)
- `.github/workflows/update.yml` — загрузка health артефактов, merge шаг, git add health.json
- `.gitignore` — исключение `!data/source_health.json`
- `prd.json` — US-C03 passes: true

### Learnings for future iterations:
- **Chunk vs round-robin sharding:** Chunk-based sharding группирует узлы по порядку парсинга — если хорошие источники парсятся первыми, drone 0 получает все их узлы. Round-robin равномерно распределяет узлы независимо от порядка.
- **Source health persistence:** Для exclusion на 24h нужна персистентность между CI-запусками. Подход с git-коммитом health-файла прост и надёжен, но требует аккуратного merge'а при параллельных дронах (используем `last_check` timestamp для разрешения конфликтов).
- **upload-artifact@v4 flattening:** Без `**` в path, артефакты flatten'ятся — `data/source_health_0.json` становится просто `source_health_0.json` в корне артефакта. Важно учитывать это при написании merge-скриптов.
- **Yield tracking:** Даже с round-robin шардингом, качество источников остаётся ключевым фактором. Source yield в телеметрии позволяет идентифицировать лучшие/худшие источники для ручного курирования.
- **Источники не шардируются на уровне fetching:** Все дроны всё ещё запрашивают все источники. Это даёт redundancy (если один дрон упадёт, другие соберут те же данные), но увеличивает нагрузку на источники. В будущем можно добавить опциональный source-level sharding.
---

## 2026-06-05 - US-C04: Расширенный скоростной тест и гео-статистика

### Что сделано

1. **Скоростной тест расширен с топ-5 до топ-20:**
   - `CHAMPION_TOP_N: 20` в `config/settings.yaml` (настраиваемый параметр)
   - `core/settings.py`: поле `CHAMPION_TOP_N: int = 20`
   - `core/engine.py`: передача `champion_top_n` в payload для Go
   - `go_core/main.go`: `ChampionTopN` в `EngineSettings`, `runChampionPhase` использует `globalSettings.ChampionTopN`

2. **Добавлены агрегированные метрики скорости:**
   - `main.py`: вычисление `avg_speed`, `median_speed`, `speed_percentile_90` из speeds всех alive nodes
   - `core/exporter.py`: новые параметры в `save_files()` → поля в `stats_*.json`
   - `merge.py`: агрегация по дронам (weighted average для avg, max для median/p90)

3. **GeoIP-based country stats:**
   - `main.py`: распределение по странам из `node.country` (GeoIP-данные от `Inspector._resolve_geo`)
   - `core/exporter.py`: `country_stats` в per-drone `stats_*.json`
   - `merge.py`: агрегация country stats со всех дронов, fallback на `_extract_country_stats()` (URL parsing)
   - `_extract_country_stats` остаётся как fallback для старых/не-GeoIP данных

4. **Расширена секция статистики на дашборде:**
   - `template.html`: новые data-атрибуты (data-avg-speed, data-median-speed, data-speed-p90)
   - `template.html`: новые строки в speed summary (Средняя/Медианная/90-й перцентиль)
   - `main.js`: `initStats()` рендерит новые поля из node-stats JSON blob
   - `merge.py:build_html()`: новые плейсхолдеры и поля в `node_stats`

### Изменённые файлы

- `config/settings.yaml` — +CHAMPION_TOP_N: 20
- `core/settings.py` — +CHAMPION_TOP_N field
- `core/engine.py` — champion_top_n в payload
- `go_core/main.go` — ChampionTopN в EngineSettings + runChampionPhase
- `main.py` — вычисление avg/median/p90 + country_stats из GeoIP
- `core/exporter.py` — новые параметры в save_files + поля в stats JSON
- `merge.py` — агрегация новых полей, улучшенные country stats
- `config/web/template.html` — data-атрибуты, расширенная speed summary
- `config/web/main.js` — рендеринг новых полей в initStats
- `index.html` — regenerated

### Learnings for future iterations:
- **Go champion phase:** `runChampionPhase` переиспользует `processSingboxBatch` с `isChampion=true`. Это запускает отдельный sing-box процесс на каждый champion-узел. При 20 чемпионах это 20 sing-box рестартов — значительное увеличение времени (20×~35s = ~700s дополнительно). Важно мониторить общее время дрона.
- **Speed stat aggregation:** Медиану и перцентили нельзя точно агрегировать между дронами без полного списка скоростей. Текущий подход (max across drones) даёт верхнюю оценку, не точное значение. Для точных метрик нужно либо слать все скорости в merge.py (дорого по данным), либо вычислять в одном дроне.
- **Country stats эволюция:** Переход от URL-parsing к GeoIP данным значительно точнее — многие узлы не имеют country-меток в URL. GeoIP через ip-api.com даёт актуальную страну VPS-хостера. Fallback на URL parsing сохранён для обратной совместимости.
- **CHAMPION_TOP_N tradeoff:** Увеличение с 5 до 20 даёт более репрезентативные данные о топ-скоростях, но каждый champion-узел тестируется отдельным sing-box процессом с 10MB загрузкой. 20 чемпионов × ~35s/batch = до 700s дополнительно к общему времени дрона.
- **index.html regeneration after template changes:** Любое изменение template.html/style.css/main.js требует регенерации index.html. Это уже учтено в workflow.
---

## 2026-06-05 - US-C06: Улучшение стилизации hero-секции и кнопок

### Что сделано

1. **Переписан .hero-actions layout (mobile-first) в style.css:**
   - БАГ: `.btn-primary` имеет `width: 100%`, что внутри flex-контейнера `.hero-actions`
     заставляло wizard-кнопку переноситься на новую строку ДАЖЕ на десктопе → кнопки
     стекались вертикально вместо горизонтального расположения.
   - Mobile (<768px): `flex-direction: column; align-items: stretch` — кнопки вертикально, полная ширина.
   - Desktop (>=768px): `flex-direction: row; flex-wrap: wrap` + `.btn-primary`/`.wizard-cta-wrap`/`.btn-wizard`
     получают `width: auto` → горизонтальное расположение по содержимому.
   - Удалён старый брейкпоинт `@media (min-width: 600px)` (justify-content only).

2. **Анимированная иконка fa-hat-wizard:**
   - `.btn-wizard.pulse-hint i` → keyframe `wizard-icon-bob` (translateY + rotate) при первом визите.
   - `.btn-wizard:hover i` → rotate(-12deg) scale(1.12) с transition.

3. **Onboard-bubble (первый визит):**
   - Уже корректно реализован в `initOnboarding()` (main.js): проверяет `localStorage sd_visited`,
     показывает bubble + pulse-hint только если не посещал. Изменений не потребовалось.

### Изменённые файлы

- `config/web/style.css` — .hero-actions mobile-first, width:auto на десктопе, wizard-icon-bob анимация
- `index.html` — regenerated
- `prd.json` — US-C06 passes: true

### Learnings for future iterations:
- **flex-wrap + width:100% child = forced wrap.** Если flex-ребёнок имеет `width:100%`, он занимает
  всю строку и со `flex-wrap: wrap` выталкивает соседей вниз. Для горизонтального layout на десктопе
  нужно явно сбрасывать `width: auto` у детей в брейкпоинте, а не только менять `justify-content`.
- **Mobile-first брейкпоинты:** базовые стили = мобильный (column/stretch), `@media (min-width: 768px)`
  переопределяет на десктоп (row/auto). Согласуется с порогом 768px из acceptance criteria.
- **fa-sparkles vs fa-wand-magic-sparkles:** в hero onboard-bubble используется fa-wand-magic-sparkles,
  в fun-panel stardust — fa-sparkles. Не путать.
---

## 2026-06-05 - US-C07: Добавление мониторинга качества проверки в Telegram-отчёт

### Что сделано

1. **Расширен send_telegram_report() в merge.py:**
   - Overall survival rate: `unique_alive / parsed * 100`, с ⚠️-алертом при <0.5%
   - Лучший/худший дрон по survival rate (max/min из drone_survival)
   - Топ-3 страны по количеству узлов (из country_stats)
   - Топ-3 источника по yield (из top_sources)
   - Разбивка причин отказов (top-5 из failure_reasons)
   - HTML-разметка Telegram: <b>, <code>, эмодзи, разделители

2. **Новые поля агрегации в main() (merge.py):**
   - `drone_survival`: per-drone alive/parsed % (из stat-файлов)
   - `failure_reasons`: агрегирует l4_failure_reasons + l7_stats со всех дронов,
     исключая ключи total/survived и не-int значения (вложенные объекты)
   - `top_sources`: заполняется из существующего ranked yield-списка (top-5)

### Изменённые файлы

- merge.py — stats dict (+3 поля), цикл агрегации stat-файлов, yield-ranking, send_telegram_report()
- prd.json — US-C07 passes: true

### Learnings for future iterations:
- **stats-файлы дрона содержат и L4, и L7 failure данные:** l4_failure_reasons (плоский dict),
  l7_stats (dict с total/survived + категории отказов). При агрегации причин отказов важно
  исключать total/survived (это счётчики, не причины) и не-int значения (l7_stats может
  содержать вложенные объекты).
- **survival rate vs L4 dropped:** В отчёте уже был l4_dropped, но не было итогового survival %.
  Overall survival = unique_alive/parsed (после дедупликации), per-drone = alive/parsed (до дедупа).
  Разные знаменатели — overall ниже из-за межшардовых дубликатов.
- **top_sources переиспользует ranked yield:** US-C03 уже считал yield-ранкинг для логов.
  US-C07 просто захватывает top-5 в stats для Telegram — без дублирования вычислений.
- **Только merge.py изменён:** US-C07 чисто на стороне отчётности, не трогает checker/Go/HTML.
  Регенерация index.html не требовалась.
---

======================================================================
# === Merged from ralph/audit-remediation line ===
======================================================================

# Ralph Progress Log
Started: Thu Jun  4 04:19:02 RTZ 2026

## Codebase Patterns
- **Go settings contract (3 files in sync):** `EngineSettings` struct in
  `go_core/main.go` <- payload built in `core/engine.py` `Inspector.process_all`
  (the `"settings"` dict) <- values read from `core/settings.py CONFIG.checking`
  which loads `config/settings.yaml`. To add a tunable for the Go core: add a JSON
  field on the struct, add the key to the engine.py payload (`CONFIG.checking.get(...)`),
  and document it under `checking:` in settings.yaml.
- **Go build needs `go mod tidy` first:** `go_core/go.sum` may be absent; the build
  fails with "missing go.sum entry" until `cd go_core && go mod tidy` is run (this is
  what engine.py does at runtime on first compile). go.sum should be committed.
- **`go vet` (Go 1.26) runs the hostport analyzer:** `fmt.Sprintf("%s:%d", host, port)`
  passed to net.Dial is flagged. Use `net.JoinHostPort(host, strconv.Itoa(port))`.
- **Local env caveat:** this machine runs Python 3.14 + pydantic 1.x; the repo pins
  pydantic==2.6.1 (uses `field_validator`). `import core.*` fails locally because
  pydantic-core 2.x has no cp314 wheel and source build needs Rust. `py_compile` is
  the reliable local Python check; full imports/pytest validate in CI (3.11/3.12).
- **L7 survival vs ranking:** a node survives the Go L7 phase if it passes the
  connectivity ping (HTTP 204). Speed is measured and stored on every survivor for
  sorting; it only filters when `speed_as_filter: true`.
---

## Thu Jun  4 - US-001 (Speed as ranking signal, not a hard filter)
- Made speed a ranking signal instead of a hard drop in the Go L7 phase (AUDIT §2.4).
- `go_core/main.go`:
  - Added `SpeedAsFilter bool` and `SpeedConcurrency int` to `EngineSettings`.
  - `processSingboxBatch` speed loop: speed is always recorded on the node (0.0 on a
    failed measurement); the node still survives. `min_speed` only drops a node when
    `speed_as_filter` is enabled. `semSpeed` concurrency is now configurable
    (`speed_concurrency`, default 12, down from the hard-coded 40 that caused uplink
    contention and understated speeds).
  - Removed the `if speed < minSpeed { return 0, false }` cut from `testHTTPSpeed`;
    the threshold decision moved to the caller. testHTTPSpeed now purely measures.
  - Fixed a `go vet` hostport finding in `checkL4` (net.JoinHostPort) so vet is clean.
- `core/engine.py`: added `speed_as_filter` and `speed_concurrency` to the settings
  payload sent to the Go core.
- `config/settings.yaml`: added `speed_as_filter: false` and `speed_concurrency: 12`
  under `checking:` with comments.
- `go_core/go.sum`: generated via `go mod tidy` (was missing; build failed without it).
- **Checks:** `go build -o /dev/null ./...` OK, `go vet ./...` clean,
  `python -m py_compile main.py merge.py core/*.py` OK. No tests/ dir yet (US-008).
- **Learnings for future iterations:** see ## Codebase Patterns above.
---

## Thu Jun  4 - US-003 (Add .gitignore for build/runtime artifacts)
- Created root `.gitignore` (AUDIT §2.12) ignoring: `data/`,
  `go_core/angra_core`, `go_core/angra_core.exe`, `*.log`, `__pycache__/`,
  `*.pyc`, `.last-branch`, `archive/`.
- Verified by creating each artifact path (data/*.json, run.log, the two
  angra_core binaries, archive/old.txt) then `git status --porcelain` — only
  `.gitignore` itself shows as untracked; the previously-untracked
  `__pycache__/`, `core/__pycache__/`, and `.last-branch` are now ignored.
- Confirmed no tracked source is caught: `git check-ignore main.py
  core/util.py go_core/main.go` matches nothing; all 33 tracked files remain.
- **Files changed:** `.gitignore` (new).
- **Learnings for future iterations:**
  - No pre-existing `.gitignore` existed; the repo had been committing source
    only. `data/` is the Python<->Go IPC handoff dir (go_in_*/go_out_*.json) and
    is purely runtime — safe to ignore wholesale.
  - `git check-ignore <path>` is the precise way to prove a path is/ isn't
    ignored; `git status --porcelain` after touching artifacts proves nothing
    leaks in.
---

## Thu Jun  4 - US-002 (Remove dead code and extract shared UUID util)
- Extracted the duplicated UUID validator into `core/util.py` as a module-level
  `is_valid_uuid(val) -> bool` (AUDIT §2.5).
- `core/parser.py`: removed `LinkParser._is_valid_uuid`, now imports
  `is_valid_uuid` from `core.util`; updated the call site in `_normalize_config`.
  Dropped the now-unused `import uuid` (parser no longer references the module;
  `uuid=uid` call-args are keyword names, not the module).
- `core/engine.py`: removed `BatchEngine._is_valid_uuid` and the two call sites
  now use `is_valid_uuid`; kept `import uuid` (still used by `uuid.uuid4()` for
  the go_in/go_out temp-file id). Removed the unused `Inspector.champion_run`
  method (champion phase lives in Go; grep confirmed zero callers in-repo).
- **Checks:** `python -m py_compile main.py merge.py core/*.py` OK.
  `python -c "import core.util"` OK (returns True/False correctly). Full
  `import core.parser, core.engine` fails LOCALLY only — pydantic 1.x vs the
  pinned 2.x `field_validator` (documented caveat); validated in CI.
- **Learnings for future iterations:**
  - Shared helpers belong in `core/util.py` now — import `is_valid_uuid` from
    there rather than re-defining. `core.util` has no heavy deps (just stdlib
    `uuid`), so it imports cleanly even on the local pydantic-mismatched env —
    handy as the one Python module you can actually `import`-test locally.
  - When removing a method that used a stdlib module, re-grep `module\.` in that
    file before deleting the import (parser's `uuid` was orphaned; engine's was
    not, due to `uuid.uuid4()`).
---

## Thu Jun  4 - US-004 (Parse once: NODES_FILE input mode + round-robin sharding)
- Added NODES_FILE env-var support to main.py (AUDIT §2.1, §2.2).
- `main.py`:
  - Added `import json` and `from core.models import ProxyNode`.
  - When `NODES_FILE` env var is set, loads pre-parsed nodes from that JSON file
    via `ProxyNode.model_validate(d)` instead of calling `fetch_and_parse()`.
  - Sharding switched from contiguous slicing (`all_nodes[i*chunk:(i+1)*chunk]`)
    to round-robin (`all_nodes[shard_index::shard_count]`) for balanced per-drone
    workloads regardless of source ordering.
  - Metrics section now uses `source_metrics` dict — populated from
    `parser.metrics` in normal mode, empty `{}` in NODES_FILE mode (source-level
    metrics aren't available when loading pre-parsed nodes).
  - Backwards compatible: when `NODES_FILE` is unset, behavior is identical to
    before.
- `parse_only.py`: already existed (untracked); now committed. Runs
  `RKNValidator.load_lists()` + `LinkParser.fetch_and_parse()` and writes
  `data/nodes.json` as a list of `model_dump(by_alias=True)` dicts.
- **Checks:** `python -m py_compile parse_only.py main.py merge.py core/*.py` OK.
- **Learnings for future iterations:**
  - The pre-existing `parse_only.py` (untracked) was already written for this
    story — needed only the main.py side to complete the feature.
  - `ProxyNode.model_validate(d)` (pydantic v2) reconstructs nodes from
    `model_dump(by_alias=True)` output correctly, including alias fields like
    `serviceName` and `aid`.
  - When splitting the parse/drone pipeline, source-level metrics
    (`parser.metrics`) aren't available in the drone — `source_metrics` defaults
    to empty dict so the metrics/dead_sources code still runs without error.
  - Round-robin slicing: `all_nodes[shard_index::shard_count]` is the idiomatic
    Python way to interleave shards; it distributes nodes from different sources
    evenly across drones vs contiguous slicing which groups same-source nodes
    together.
---

## Thu Jun  4 - US-005 (GeoIP efficiency: dedup hosts and /24 country cache)
- Deduplicated hosts before DNS resolution and added /24 subnet country cache
  to reduce redundant ip-api lookups (AUDIT §2.3).
- `core/engine.py`:
  - Added `BatchEngine._geo_cache_key(ip)` static method: returns `/24` network
    string for IPv4 (e.g. `1.2.3.0/24`), exact IP for IPv6. Neighbouring IPs in
    the same /24 almost always share a country, so one ip-api lookup covers the
    whole subnet.
  - Rewrote `Inspector._resolve_geo`: builds a `host -> [nodes]` map first,
    resolves only unique hostnames via DNS, then maps IPs back to all nodes.
    Previously called `gethostbyname` for every node including duplicates.
  - GeoIP fetch list is now deduplicated by /24 cache key (not exact IP) —
    after the first IP in a /24 is looked up, subsequent IPs in the same /24
    hit the cache and skip ip-api entirely.
  - Cache store uses `_geo_cache_key(ip_key)` so all IPs in the same /24
    benefit from one lookup.
  - Improved logging: shows `nodes -> unique_hosts`, IP count, cache hits,
    and number of actual ip-api fetches — provably quantifies the reduction.
- **Checks:** `python -m py_compile main.py merge.py parse_only.py core/*.py`
  OK. `import core.util` OK. Unit-tested `_geo_cache_key` logic (same-/24
  sharing, cross-/24 isolation, IPv6 exact, garbage fallback). Full
  `import core.engine` fails locally per documented pydantic 1.x/2.x caveat;
  will pass in CI (3.11/3.12).
- **Learnings for future iterations:**
  - `dict.setdefault(key, default)` is the clean Python idiom for building a
    `host -> [nodes]` multi-map in one pass.
  - `ipaddress.IPv4Network(f"{addr}/24", strict=False)` is the correct way to
    get the /24 network from an IPv4 address — `strict=False` allows any host
    bits. `str(net)` returns `"x.y.z.0/24"`.
  ## Thu Jun  4 - US-006 (Rolling pool of historically-working nodes)
- Implemented a rolling pool of historically-working nodes (AUDIT §4.1).
- `core/parser.py`:
  - Added `LinkParser.parse_link(line)` static method — generic URI dispatcher that
    auto-detects protocol prefix and delegates to the correct parser (parse_vless,
    parse_vmess, parse_trojan, parse_ss, parse_hy2). Returns `ProxyNode | None`.
    Handles `hysteria://` alias (redirects to parse_hy2). Essential for round-tripping
    pool URIs back into checkable ProxyNodes.
- `main.py`:
  - After fresh nodes are loaded (from sources or NODES_FILE), loads `data/pool.json`
    and parses each pooled URI via `LinkParser.parse_link`.
  - Pool nodes not already in the fresh set (dedup by `strict_id`) are appended to
    `all_nodes` with `is_bs` set via `RKNValidator.check_bs`.
  - Pool loading happens BEFORE the empty-nodes abort check — so a drone can still
    operate on pool-only nodes when fresh sources are down.
  - If pool.json is missing or corrupt, logs a warning and continues.
- `merge.py`:
  - Added `load_pool(path)` — loads pool.json, returns `[]` on missing/corrupt.
  - Added `save_pool(pool, path)` — creates `data/` dir if needed, writes JSON.
  - Added `update_pool(alive_uris, path)` — the core pool maintenance logic:
    1. Pool nodes found in `alive_uris` → fail_count reset to 0, last_seen updated.
    2. Pool nodes NOT in `alive_uris` → fail_count incremented; evicted at >= 3.
    3. Alive URIs not yet in pool → added with fail_count=0, last_seen=now.
  - New `③ POOL` group in `main()`: reads alive URIs from the merged output files
    (`sub_all.txt`, `sub_bs.txt`, `sub_chs.txt`), calls `update_pool`, logs sizes.
  - Subsequent group numbers renumbered (④ BUILD, ⑤ NOTIFY).
- **Checks:** `python -m py_compile main.py merge.py core/parser.py core/engine.py
  core/exporter.py` OK. `python -c "import core.util"` OK. Pool logic verified with
  7 table-driven unit cases (first-run, fail-increment, eviction-at-3, recovery-reset,
  new-node-addition, differential-eviction, empty-run).
- **Learnings for future iterations:**
  - `LinkParser.parse_link` is now the canonical single-URI entry point — use it
    anywhere a raw proxy URI needs to become a ProxyNode (pool loading, manual
    injection, RU verdict ingestion, etc.).
  - Pool persistence lives in `data/pool.json` (gitignored). For CI, `merge.py`
    produces it and `main.py` consumes it in the next run. An `actions/cache` or
    `upload/download-artifact` step is needed in the workflow to carry pool.json
    between runs — the Python code is CI-transport-agnostic.
  - The pool's fail_count tracks CONSECUTIVE failures (reset on any success). This
    is resilient to transient network issues — a good node must fail 3 runs IN A ROW
    to be evicted.
  - `update_pool` mutates the `alive_uris` set in place (`.discard()` as it matches
    existing entries) — callers should pass a copy if they need the original set
    afterwards.
  - Pool URIs round-trip through `Exporter._build_url` → `LinkParser.parse_link`.
    This is the same parse-export pair used everywhere, so pool nodes are checker-
    compatible by construction.
---

---
- The `_GEO_CACHE` is a class-level `Dict[str, str]` on `BatchEngine`,
  shared across all `Inspector` instances within a process. The /24 key
  strategy means once ANY drone resolves one IP in a /24, all subsequent
  nodes in that /24 (even across different batches) reuse the result.
---

## Thu Jun  4 - US-007 (Publish output to a dedicated branch, no force-push on main)
- Replaced amend+force-push on `main` with normal commit+push to `gh-pages`
  orphan branch (AUDIT §2.6).
- `.github/workflows/update.yml`:
  - Replaced "⭰ Sync & Deploy" step with "⭰ Publish to gh-pages".
  - Saves generated files (`sub_all.txt`, `sub_bs.txt`, `sub_chs.txt`,
    `index.html`, `vercel.json`) to `/tmp/publish/` before switching branches.
  - Creates `gh-pages` as an orphan branch on first run, otherwise cleans and
    re-populates it.
  - Uses normal `git commit` + `git push origin gh-pages` — **no amend, no
    force-push** against main (or any branch).
- `vercel.json`: Added `//` comment noting the deploy target branch
  (`gh-pages`) and that it's synced by CI.
- `README.md`: Added "🚀 Деплой (Deployment)" section documenting the
  `gh-pages` → Vercel pipeline and the Vercel dashboard setting needed.
- `AUDIT.md` §2.6: Updated to note the fix (US-007, выполнено).
- **Checks:** `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/update.yml', encoding='utf-8'))"` OK
  (2 jobs: matrix-crawler, matrix-merge). `python -m py_compile main.py merge.py
  parse_only.py core/*.py` OK. `vercel.json` valid JSON. No `amend` or `force`
  in the workflow (except our own comment).
- **Learnings for future iterations:**
  - GitHub Actions `actions/checkout@v4` with `fetch-depth: 0` fetches all
    branches — `git ls-remote --heads origin gh-pages` is the reliable way to
    check if a remote branch exists before trying to check it out.
  - `git checkout --orphan gh-pages` creates a branch with no history; combine
    with `git rm -rf --quiet . || true` to get an empty working tree on first
    run. Subsequent runs use `find . -maxdepth 1 ... -exec rm -rf {} \;` to
    clean the working tree while preserving `.git`.
  - Vercel's `vercel.json` lives on the deployed branch (`gh-pages`), not on
    the source branch (`main`). The CI step copies it to `gh-pages` each run so
    it stays in sync — manual edits to `vercel.json` in `main` propagate
    automatically on the next CI run.
  - When switching branches mid-job, save generated artifacts to a temp
    location first (`/tmp/publish/`) — `git checkout` replaces the working
    tree, so any files only in the working tree (not committed) are lost.
---


## Thu Jun  4 - US-008 (Parser tests and linters in CI)
- Added regression tests for the link parser and CI lint infrastructure (AUDIT §3.1, §3.2).
- `tests/__init__.py`: empty package marker.
- `tests/test_parser.py`: 47 table-driven test cases organised by protocol class:
  - **VLESS** (6 valid + 6 garbage): tcp+reality, ws+tls, grpc+serviceName;
    invalid UUID, bad host, missing port, garbage word, bad PBK, missing UUID.
  - **VMess** (2 valid + 4 garbage): tcp+tls, ws; invalid UUID, bad host,
    garbage word, missing port.
  - **Trojan** (2 valid + 3 garbage): tls, ws+host; missing password, bad host,
    garbage word.
  - **SS** (2 valid + 4 garbage): aes-256-gcm, 2022-blake3; invalid method,
    bad host, garbage word, non-ss prefix.
  - **HY2/Hysteria2** (3 valid + 3 garbage): hy2://, hysteria2://, obfs;
    missing password, bad host, garbage word.
  - **parse_link dispatcher** (10 cases): auto-detects vless/vmess/trojan/ss/
    hy2/hysteria2/hysteria(alias); returns None for unknown, empty, garbage,
    comments.
  - **_is_garbage filter** (3 cases): flags known garbage words, passes clean
    URIs, case-insensitive.
  - Tests skip gracefully (pytest skipif) when pydantic 2.x is unavailable
    (cp314 local dev) — `pytest -q` exits 0 with 47 skipped. In CI (Python
    3.11) they run fully.
- `requirements-dev.txt`: pytest>=8, ruff>=0.8.
- `.github/workflows/ci.yml`: triggers on push/PR to main; runs ruff on
  `core/` + `tests/`, pytest, and go vet.
- **Checks:** `python -m py_compile main.py merge.py parse_only.py core/*.py
  tests/*.py` OK. CI YAML valid (yaml.safe_load). `pytest -q` → 47 skipped
  (exit 0). `go vet ./...` clean.
- **Learnings for future iterations:**
  - `LinkParser.parse_link` now has full test coverage — it's the canonical
    single-URI entry point. Any new protocol support should add corresponding
    table-driven cases to `TestParseLink` and the protocol-specific classes.
  - The PBK validation in `_normalize_config` requires base64url-encoded
    32-byte keys (40-46 chars). Synthetic test values must satisfy both length
    and decode checks.
  - `LinkParser.decode_base64` auto-pads missing `=` — so tests can provide
    unpadded base64 (matching real-world VMess wire format).
  - ruff has no config file yet (uses defaults); if the team wants stricter
    rules, add a `ruff.toml` or `[tool.ruff]` section in pyproject.toml.
  - For CI: `actions/setup-python@v5` with `cache: 'pip'` works alongside
    `requirements-dev.txt` — pip installs both files in sequence.
---

## Thu Jun  4 - US-009 (Cache whitelist lists and log list age)
- Added whitelist caching with fallback so the BS/Nightbird class survives a
  temporary outage of upstream whitelist sources (AUDIT §5.1.3).
- `core/validator.py`:
  - Cache dir `data/whitelist_cache/` (gitignored via `data/`), with
    `domains.txt` and `cidr.txt` holding the last successfully fetched lines.
  - `_parse_lines(text)`: extracts lowercased non-comment lines (was inline
    twice, now shared).
  - `_save_cache(path, lines)` / `_load_cache(path)`: write/read cache files,
    tolerant of IO errors (warn + continue).
  - `_cache_age_str(path)`: returns mtime-based age (e.g. "3.2ч") for "list age"
    logging.
  - `_resolve_with_cache(fetched, cache_path, label)`: if fetch produced lines →
    save to cache; if empty → load cache (warn with age) or warn empty+no-cache.
  - `load_lists` now logs per-category source success counts (e.g. "домены 1/2"),
    routes both domain and CIDR results through `_resolve_with_cache`, and the
    final empty-bases warning notes "(источники И кэш)".
- **Checks:** `python -m py_compile main.py merge.py parse_only.py core/*.py` OK.
  Cache save/load round-trip verified standalone (lowercasing, comment skip,
  age computation). Full `import core.validator` fails LOCALLY only per the
  pydantic 1.x/2.x caveat; passes in CI (3.11/3.12). ruff not installed locally.
- **Learnings for future iterations:**
  - Whitelist cache lives in `data/whitelist_cache/` — same gitignored `data/`
    dir as the pool (US-006) and Go IPC. For CI persistence across runs it needs
    an `actions/cache` or artifact step, like pool.json — the Python code is
    transport-agnostic.
  - Domains and CIDR lists fall back INDEPENDENTLY: if domain sources are up but
    CIDR sources are down, only CIDR falls back to cache. This is more resilient
    than an all-or-nothing fallback.
  - `os.path.getmtime` gives a cheap "list age" without storing a timestamp
    inside the cache file.
---

## Thu Jun  4 - US-010 (RU-verified subscription scaffolding)
- Added code-only scaffolding for an RU-verified subscription (AUDIT §5.1).
- `core/models.py`: added `ru_verified: bool = False` to `ProxyNode` (defaults
  False → backwards compatible with old NODES_FILE / pool dumps).
- `core/exporter.py`: `save_files` now also computes `nodes_ru = [n for n in nodes
  if n.ru_verified]` and writes `sub_ru{suffix}.txt`
  (title "Scarlet Devil | Remilia (RU-verified)") alongside all/bs/chs.
- `main.py`: added module-level `apply_ru_verdict(nodes)` — env-gated by
  `RU_VERDICT_FILE`. Reads a JSON verdict (bare list of strict_ids, or
  `{"verified":[...]}` / `{"ids":[...]}`), flags matching alive nodes
  `ru_verified=True`. Unset/missing file → nothing flagged → empty sub_ru
  (no failure). Called after dedup, before export.
- `merge.py`: MERGE group now also merges `sub_ru_*.txt` shards into `sub_ru.txt`
  (always produced, header-only when empty). Pool group unchanged — RU nodes are a
  subset of sub_all so already counted in alive_uris.
- `vercel.json`: added rewrite `/sub/ru -> /sub_ru.txt`.
- `.github/workflows/update.yml`: publish step copies/adds/commits `sub_ru.txt` to
  gh-pages; added a documenting comment on `RU_VERDICT_FILE` in the drone step.
- `README.md`: new "🇷🇺 RU-проба" section documenting the ingestion contract and how
  to run an external RU probe worker. `AUDIT.md` §5.1 plan item marked [~] with note.
- **Checks:** `python -m py_compile main.py merge.py parse_only.py core/*.py` OK.
  YAML (update.yml) + JSON (vercel.json) load OK. `import core.util` OK.
  `pytest -q` → 47 skipped (exit 0, pydantic caveat). Go untouched.
- **Learnings for future iterations:**
  - The RU worker identifies nodes by `strict_id` (proto:server:port:ident:transport),
    computed via `LinkParser.parse_link(uri).strict_id` — the same canonical id used by
    the pool (US-006) and dedup. Verdict files therefore round-trip through the same
    parse path; no new id scheme was introduced.
  - `sub_ru` is built per-shard in the drone (Exporter), then merged — same fan-out/merge
    pattern as sub_all/bs/chs. The upload glob `data/sub_*.txt` already captured the new
    shard files; no artifact-path change was needed.
  - Adding a new ProxyNode bool field is safe for NODES_FILE/pool round-trips because
    pydantic v2 fills defaults for absent keys on `model_validate`.
  - To wire RU verdicts into CI, add a step before "Execute Shard Protocol" that fetches
    the worker's JSON and exports `RU_VERDICT_FILE`; the code is transport-agnostic.
---
