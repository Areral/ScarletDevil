# Ralph Progress Log
Started: 2026-06-04
Branch: ralph/website-overhaul
---

## Codebase Patterns

### Website Generation
- `merge.py:build_html()` reads `config/web/template.html`, injects `{{INJECT_CSS}}` and `{{INJECT_JS}}` inline, substitutes `{{UPDATE_TIME}}`, `{{PROXY_COUNT}}`, `{{MAX_SPEED}}`, `{{SUB_LINK}}`
- Template uses `{{UPPERCASE}}` placeholder syntax (NOT Handlebars `{{variable}}` — these are simple string replacements)
- CSS is in `config/web/style.css`, JS in `config/web/main.js` — both injected inline into the final `index.html`
- `vercel.json` handles URL rewrites: `/sub` → `/sub_all.txt`, `/sub/bs` → `/sub_bs.txt`, etc.

### Subscription File Format
- Lines starting with `#` are metadata (profile-title, profile-update-interval)
- Subscription URLs are one per line, ordered by speed (fastest first from Exporter)
- `Exporter.save_files()` generates per-shard files like `sub_all_{i}.txt`, `sub_bs_{i}.txt`, etc.
- `merge.py:merge_subscription_files()` deduplicates across shards (VMess-aware dedup via `_vmess_dedup_key`)
- **Gotcha:** `sorted(unique_map.values())` re-orders alphabetically, destroying speed sort — FIXED (removed sorted)

### Exporter Architecture
- `Exporter._build_url(node, name)` builds the subscription URL per protocol (vless/vmess/trojan/ss/hy2)
- Node filtering: `is_bs` field for BS class, `ru_verified` for RU-verified, `protocol` field for protocol name
- Protocol values: `"vless"`, `"vmess"`, `"trojan"`, `"ss"`, `"hysteria2"` (lowercase)

### Config Pattern
- `config/settings.yaml` loaded via pydantic-settings in `core/settings.py` → singleton `CONFIG`
- `CONFIG.app` has `public_url`, `channel_tag`; `CONFIG.TG_BOT_TOKEN`, `CONFIG.TG_CHAT_ID` for Telegram

---

## 2026-06-04 (prior session) - US-W01 / US-W08 (partial)
- **Exporter** (`core/exporter.py`): Added protocol-based node filtering and file generation:
  - `nodes_vless`, `nodes_vmess`, `nodes_trojan`, `nodes_ss`, `nodes_hy2` list comprehensions
  - New file tuples in save_files: `sub_vless{suffix}.txt`, `sub_vmess{suffix}.txt`, etc.
- **Merge** (`merge.py`):
  - Removed `sorted()` from `merge_subscription_files` — now preserves insertion order (speed sort)
  - Added 5 new `merge_subscription_files()` calls for protocol-specific patterns
- **Remaining for US-W01:** vercel.json routes, modal window with protocol cards
- **Remaining for US-W08:** build_html() statistics placeholders ({{NODE_STATS_JSON}}, per-protocol counts)

---

## 2026-06-04 - US-W01 (completed)
- **What was implemented:**
  - vercel.json: Added 5 rewrite rules for protocol subscriptions (/sub/vless → sub_vless.txt, /sub/vmess, /sub/trojan, /sub/ss, /sub/hy2)
  - template.html: Added 5 protocol subscription cards to the configs-modal (VLESS, VMess, Trojan, Shadowsocks, Hysteria2) with Font Awesome icons and Russian descriptions
  - style.css: Added CSS color classes for each protocol card (vless=purple, vmess=blue, trojan=gold, ss=green, hy2=sakura) with hover effects and badges
- **Files changed:** vercel.json, config/web/template.html, config/web/style.css
- **Backend already done (prior session):** exporter.py protocol file generation, merge.py protocol merge calls, sorted() removal
- **Learnings for future iterations:**
  - The vercel.json header regex `/(.*sub.*)` already covers all `/sub/*` paths — no new header rules needed for protocol routes
  - The `copySub()` JS function constructs URL as `BASE_URL + path` — vercel routes and JS paths must agree
  - Sub-card CSS follows a consistent pattern: border color class, ::before accent bar, :hover glow, badge class
  - All 8 subscription cards are now in a single scrollable grid; US-W04 will redesign this into a tabbed layout
---

## 2026-06-04 - US-W02
- **What was implemented:**
  - Added 11 new apps to APP_DATABASE: FlClash, Mihomo (Clash Meta), AmneziaWG, SagerNet, FoXray, Surge, Stash, Sing-box CLI, MikroTik (RouterOS), OpenWRT, NekoRay
  - Added `protocols` field (array of supported protocols) to all 22 app entries
  - Added new `router` platform with 4 apps: mikrotik, openwrt, singbox_cli, mihomo
  - Expanded `androidtv` from 1 to 3 apps: v2rayng, sagernet, flclash
  - Added router tab button in template.html
  - Added protocol badge display in updateUI() with <div id="protocol-display">
  - Added .proto-tag CSS class for protocol badge styling (JetBrains Mono, sakura color)
  - Each new app has a 5+ step guide with specific button/menu names
  - App expansion across all existing platforms (e.g., windows: +flclash, +mihomo, +amneziawg, +singbox_cli, +nekoray)
- **Files changed:** config/web/main.js, config/web/template.html, config/web/style.css
- **Learnings for future iterations:**
  - APP_DATABASE keys use lowercase_with_underscores convention
  - Each app entry = {name, icon, url, protocols[], guide} — protocols is an array for future filtering
  - updateUI() is the central render function: it rebuilds the app grid, sets download URL, name, instruction, and now protocols
  - The `protocol-display` div sits between the download button and "2. Импорт и запуск" heading
  - For router apps (MikroTik, OpenWRT), the guide format differs: it includes CLI commands and config snippets
  - Font Awesome 6 Free icons: verify availability before using — prefer solid style (fa-solid)
  - Quality checks for JS: `node --check` catches syntax errors without executing DOM-dependent code
---

## 2026-06-04 - US-W03
- **What was implemented:**
  - Added `<div class="guide-tip">` block (Совет) for all 22 apps with specific, actionable advice
  - Added `<div class="guide-troubleshoot">` block (Решение проблем) for all 22 apps with 2-3 common issues and solutions
  - Added `<div class="guide-config">` block with copy-paste CLI configs for router apps (mikrotik WireGuard config, openwrt OpenClash install script)
  - Fixed v2raytun from 4 to 5 steps (split "обновите и выберите" into two distinct steps)
  - Enhanced existing guides: more specific button names (e.g., «OK» vs «Ок»), platform-appropriate instructions, concrete DNS/port advice
  - Added CSS for `.guide-tip` (green left-border, cyber-green accent), `.guide-troubleshoot` (amber left-border, facc15 accent, nested ul/li styling), `.guide-config` (dark code block with label)
- **Files changed:** config/web/main.js, config/web/style.css
- **Platform patterns applied:**
  - Android apps: instructions emphasize clipboard import and battery optimization (v2rayNG, SagerNet, FoXray, v2RayTun)
  - iOS apps: instructions emphasize Subscribe URL and iOS-specific limitations (V2Box, Shadowrocket, Streisand, Stash, Surge)
  - Router apps: instructions include CLI copy-paste configs and system-level troubleshooting (MikroTik, OpenWRT)
  - CLI apps: instructions include systemd/service setup and permission troubleshooting (Sing-box CLI, Mihomo)
- **Learnings for future iterations:**
  - Guide blocks are injected as raw HTML via `updateUI()` → `innerHTML` — no template engine involved
  - Each guide's tip/troubleshooting should be app-specific, not generic — the value is in knowing exact menu paths and common failure modes for that specific client
  - The CSS for nested lists inside `.guide-troubleshoot` needs explicit `ul/li` selectors since the base `.text-block` styles don't apply inside `.instruction-box`
  - Router config blocks use `<pre>` for preserved formatting — `white-space: pre-wrap` is essential for mobile responsiveness
  - Guide bloat is real: each app now has ~40-60 lines of HTML. If this grows further, consider extracting guides to a separate data file
---

## 2026-06-04 - US-W04
- **What was implemented:**
  - **configs-modal** (template.html): Полностью переработан на двухвкладочный layout:
    - Вкладка «По классу»: 4 карточки — Nightbird (БС), Vampire Dash (ЧС), Gungnir (MIX), Remilia (RU-verified)
    - Вкладка «По протоколу»: 5 карточек — VLESS, VMess, Trojan, Shadowsocks, Hysteria2
    - Каждая карточка имеет кнопки «Копировать URL» и «QR»
    - RU-verified карточка визуально выделена золотой рамкой и пометкой «Remilia»
    - Добавлена область отображения QR-кода с кнопкой «Назад»
  - **QR-генератор** (main.js): Самописный, без внешних зависимостей (~250 строк):
    - GF(256) арифметика (exp/log таблицы, примитивный полином 0x11D)
    - Кодирование в Byte mode с выбором версии (1-6), EC Level L
    - Полиномиальный Reed-Solomon для коррекции ошибок
    - Построение QR-матрицы: finder patterns, timing, alignment, форматная информация
    - Оценка 8 масок по 4 правилам штрафа, выбор лучшей
    - Рендеринг на canvas с отступами
  - **Clipboard fallback** (main.js): `navigator.clipboard.writeText` → `execCommand('copy')` → `prompt()` для ручного копирования
  - **Modal UX**: Закрытие по Escape, закрытие по клику на overlay
  - **Backend plumbing** (US-W04 + prep for US-W08):
    - `ProxyNode.ru_verified: bool = False` в models.py
    - `nodes_ru` фильтрация в exporter.py, генерация `sub_ru_*.txt`
    - `merge_subscription_files` для `sub_ru_*.txt` в merge.py
    - vercel.json: rewrite `/sub/ru` → `sub_ru.txt`
  - **CSS** (style.css): `.config-tabs`/`.config-tab-btn`, `.sub-actions`/`.sub-action-btn.copy-btn`/`.qr-btn`, `.sub-card.ru-verified`/`.badge-ru`, `.qr-display`/`.qr-container`/`.qr-back-btn`, `.modal-wide`, responsive sub-grid (2 колонки ≥500px, 1 колонка mobile)
- **Files changed:** config/web/template.html, config/web/main.js, config/web/style.css, core/models.py, core/exporter.py, merge.py, vercel.json
- **Learnings for future iterations:**
  - QR-генератор использует таблицы GF(256) с примитивным полиномом 0x11D (x^8+x^4+x^3+x^2+1). EXP[0..511], LOG[0..255] для быстрого умножения. gfInv(a) = EXP[255 - LOG[a]].
  - Форматная информация (15 бит) предвычислена для EC Level L × 8 масок. Бит 14 → [8,0], бит 0 → [0,8] (вокруг верхнего левого finder'а) и зеркально.
  - В modal-wide (max-width: 620px) сетка карточек подписок 2×N на десктопе, 1×N на мобильных (breakpoint 500px).
  - `execCommand('copy')` требует временного textarea в DOM, фокуса и select(). Работает как fallback когда clipboard API недоступен (HTTP, старые браузеры).
  - `event.stopPropagation()` на кнопках внутри sub-card критичен — без него клик по кнопке всплывает на родительский div.
  - `config-tab-content` скрывается/показывается через `style.display`, а не классы — чтобы не конфликтовать с CSS-анимациями reveal.
  - Для QR area: кнопки табов блокируются (`pointerEvents: none`, `opacity: 0.4`) при показе QR, восстанавливаются при возврате.
  - RU-verified plumbing добавлен минимально: модель, exporter, merge, vercel. Логика определения `ru_verified` (какие узлы считать проверенными из РФ) оставлена для US-W08.
---

## 2026-06-04 - US-W05
- **What was implemented:**
  - **Mobile navigation:** Added hamburger button (visible ≤1024px) that opens a slide-out panel with section links and fun effects. Panel closes on link click, close button, or overlay click. Animated hamburger → X transition.
  - **Fun-panel mobile:** Changed from `display: none` to smaller buttons (36px, rounded squares) at bottom-left. Effects synced between desktop and mobile via paired button click forwarding.
  - **Meta/OG tags:** Added `og:title`, `og:description`, `og:image`, `og:type`, `og:url`, `twitter:card`, `twitter:title`, `twitter:description`, and `description` meta tag.
  - **Favicon:** Emoji-based inline SVG favicon (🦇) — no external file needed.
  - **Skip-link:** Added hidden skip-link that appears on focus (top of page) for screen reader keyboard navigation. Links to `#main-content`.
  - **Aria-labels:** Added to all nav buttons, fun-panel buttons, mobile nav links, and dynamically to app cards via MutationObserver. App cards now have `role="button"`, `tabindex="0"`, and keyboard support (Enter/Space).
  - **Removed dead `{{SUB_LINK}}`:** Removed `.replace("{{SUB_LINK}}", ...)` from `merge.py:build_html()`.
  - **Fixed Android TV detection:** Changed `ua.includes("tv") || window.innerWidth > 1000` → `ua.includes("tv")` only. Removed spurious width-based heuristic.
  - **PWA manifest:** Created `manifest.json` with theme_color (#e11d48), background_color (#0f0508), display: standalone, emoji icon.
  - **External links:** Added `rel="noopener noreferrer"` to Telegram link, footer link, and download button.
  - **Local time conversion:** Added `{{UPDATE_TIME_ISO}}` to merge.py, `data-utc` attribute on time element, and `initLocalTime()` JS function that converts ISO timestamp to user's local timezone via `toLocaleString('ru-RU', ...)`.
- **Files changed:** merge.py, config/web/template.html, config/web/style.css, config/web/main.js, vercel.json, prd.json, progress.txt
- **Files created:** config/web/manifest.json
- **Pre-existing issues (not caused by this change):**
  - Go build fails: `missing go.sum entry for golang.org/x/net/proxy` (environment, no Go changes made)
  - Pydantic import: `field_validator` not in older pydantic version (environment, no Python model changes)
  - update.yml YAML validation: cp1251 encoding (not UTF-8), pre-existing
- **Learnings for future iterations:**
  - `vercel.json` catch-all `/(.*)` → `/index.html` rewrites everything — any new static file (manifest.json) needs an explicit route before the catch-all
  - Mobile nav pattern: hamburger → slide-out panel with overlay. Panel uses `transform: translateX(100%)` + `transition` for smooth animation. Overlay uses `opacity` transition
  - Fun button sync: mobile and desktop buttons share state via `.click()` forwarding + class mirroring — avoids duplicating effect logic
  - `sys-log-time` pattern: server renders ISO timestamp in `data-utc`, client converts to local via `new Date()` + `toLocaleString()`
  - Accessibility for dynamic content: `MutationObserver` on the app grid adds `aria-label`, `role`, `tabindex`, and keyboard handlers to app cards as they're created
  - `initScrollSpy()` now handles both desktop nav buttons AND mobile nav links (added `mobileLinks` to the observer)
  - The `{{SUB_LINK}}` placeholder was dead code — template.html never used it, it was just a no-op replacement in merge.py
  - Emoji favicon via data URI: `<link rel="icon" href="data:image/svg+xml,...">` — works in all modern browsers, no extra file needed
  - `window.innerWidth` heuristic for Android TV was unreliable (tablets, landscape phones would falsely trigger). Pure userAgent check (`ua.includes('tv')`) is the correct approach
---

## 2026-06-04 - US-W06
- **What was implemented:**
  - **Scanline overlay:** CRT scanline effect (`repeating-linear-gradient`) toggleable via fun-panel button (fa-barcode). Intensity-responsive (denser lines at Extreme).
  - **Enhanced scarlet mist:** Multi-layered radial gradients (3 layers + ::before with 2 drift layers). Mist particles spawn at Extreme intensity (MutationObserver on mist/body class changes).
  - **Glitch + neon focus:** `glitch-skew` animation on h1/h2 hover with red/blue text-shadow split. `*:focus-visible` neon outline with sakura glow. `glitch-clip` keyframe reserved.
  - **Custom scrollbar + selection:** `::-webkit-scrollbar` styled in touhou-crimson with rounded thumbs. Firefox `scrollbar-width: thin` + `scrollbar-color`. `::selection` in touhou-red with glow.
  - **Typewriter sys-log:** `initTypewriter()` stores original HTML, clears lines, types character-by-character (15-40ms per char, 200ms pause between lines). HTML-aware (skips tags as units). Blinking cursor (`▮`) with `blink-cursor` animation appended to current line.
  - **Effect intensity switch:** M/N/E cycle button at bottom-left (top-left on mobile). Body classes: `fx-minimal` (hides scanline/stardust/mist-particles, disables glitch/border-anim), default Normal, `fx-extreme` (faster mist, glitch infinite, full border opacity, denser scanlines, mist particles). Saved to localStorage `sd_fx_intensity`.
  - **Enhanced color palette:** More saturated :root variables (bg-void from #0f0508→#0a0306, accent from #f43f5e→#fb3b5c). Glow variant variables (--touhou-red-glow, --touhou-purple-glow, etc.). Third radial gradient on body. Theme foundation variables (--theme-glow-primary, etc.).
  - **Animated sub-card border:** Changed `.sub-card` accent bar from `::before` pseudo-element to `border-left: 4px solid`. Repurposed `::before` for conic-gradient rotating border (`@keyframes border-spin`, 4s linear). `::after` provides inner mask. Activates on hover (`animation-play-state: running`). RU-verified card has golden variant.
  - **Star dust canvas:** Canvas-based particle system (80 particles, 0.5-2px radius). Sine-wave pulsing alpha. Glow halos (3x radius at 15% opacity). `requestAnimationFrame` loop. Toggleable via fun-panel (fa-sparkles). Saved to localStorage `sd_fx_states`.
  - **CSS theme variables:** Full `[data-theme="midnight"]` (blue palette: bg-void #020510, accents shifted to blues/indigos) and `[data-theme="sakura"]` (pink palette: bg-void #12050a, accents shifted to pink/rose) blocks with all variable overrides.
  - **LocalStorage persistence:** All effect states (mist, danmaku, bats, scanline, stardust) saved as JSON in `sd_fx_states`. Intensity level saved as integer in `sd_fx_intensity`. States restored on page load.
  - **Mobile parity:** Scanline and stardust buttons added to mobile nav fun section. Synced with desktop buttons via existing click-forwarding pattern.
- **Files changed:** config/web/style.css, config/web/template.html, config/web/main.js, prd.json, progress.txt
- **Learnings for future iterations:**
  - `MutationObserver` on body class changes is the cleanest way for intensity-dependent effects to react to changes without tight coupling between initFXControl and every effect system
  - The sub-card accent bar migration (::before → border-left) freed up ::before for the animated border. Use `border-left` for simple accent bars when both pseudo-elements are needed for other effects
  - Conic-gradient rotating border via `::before` with `::after` inner mask: works cross-browser without `@property`. Set `animation-play-state: paused` by default, `running` on hover
  - Typewriter animation needs HTML-awareness: skip from `<` to `>` as a unit to avoid breaking tag structure. Insert cursor span by removing/re-adding it to current line
  - Canvas stardust: resize canvas on window resize. Use `canvas.classList.contains('active')` as the animation gate (checked each frame) — cleaner than external flag variables
  - Theme system via `[data-theme="..."]` attribute selectors: no JS needed to switch themes, just set `document.documentElement.setAttribute('data-theme', 'midnight')`. The base :root provides theme foundation variables for components to reference
  - The `glitch-clip` keyframe is defined but unused by default — reserved for future use with `data-text` attributes on headings
  - `saveFXStates()` helper called after every effect toggle keeps localStorage in sync. JSON.parse with fallback `{}` on init prevents errors from corrupted storage
---

## 2026-06-04 - US-W07
- **What was implemented:**
  - **FAQ section:** 12 вопросов-ответов с использованием нативного HTML `<details>/<summary>` accordion паттерна. Каждый вопрос имеет иконку Q и анимированную стрелку. Охвачены: что такое проект, как работает проверка, БС/ЧС отличия, обновление, выбор клиента, Allow Insecure, неработающие узлы, автообновление, безопасность, VLESS Reality vs обычный, роутеры, полная блокировка.
  - **Stats section:** CSS bar-charts для распределения по протоколам (VLESS/VMess/Trojan/SS/HY2), по классам (BS/CHS/RU), top-10 стран по количеству узлов, сводка скорости. Все данные рендерятся из data-атрибутов заполняемых merge.py.
  - **Protocol Comparison section:** Таблица сравнения 5 протоколов по критериям: скорость, обфускация, стабильность, рекомендация. С цветовыми rating-звёздами и рекомендательными бейджами.
  - **merge.py build_html():** Обновлён для приёма stats dict с 8 счётчиками (bs/chs/ru/vless/vmess/trojan/ss/hy2_count) и country_stats JSON. Добавлены замены для 8 новых плейсхолдеров + {{COUNTRY_STATS_JSON}}.
  - **merge.py main():** Захват всех return-значений merge_subscription_files() для подсчёта протоколов. Добавлена `_extract_country_stats()` — извлечение 2-буквенных кодов стран из #фрагментов подписных URL, top-10 сортировка.
  - **Navigation:** 3 новых пункта в боковой навигации (faq, stats, protocols) и в мобильном меню.
- **Files changed:** config/web/template.html, config/web/style.css, config/web/main.js, merge.py, prd.json, progress.txt
- **Learnings for future iterations:**
  - `<details>/<summary>` — нативный HTML accordion без JS. Стилизация через `::-webkit-details-marker { display: none }` и `::marker { display: none; content: '' }`. Анимация стрелки через `[open] .arrow { transform: rotate(180deg) }`.
  - CSS bar-чарты: `chart-bar-fill` с процентной шириной внутри `chart-bar-track`. Градиентная заливка + `::after` с белым полупрозрачным overlay для глянца. Переход `width 1s cubic-bezier(0.2, 0.8, 0.2, 1)` даёт плавную анимацию при загрузке.
  - Статистика стран извлекается из `#FRAGMENT` подписных URL через regex `#(\w{2})[\s\-_]`. Не все узлы имеют страновую метку — это best-effort извлечение.
  - `merge_subscription_files()` уже возвращает int (количество узлов) — достаточно было просто захватить return values вместо добавления новых вызовов.
  - Статистическая сетка использует `grid-template-columns: 1fr 1fr` на ≥768px с `stats-wide` (grid-column: 1 / -1) для страны на всю ширину.
  - Все 8 новых плейсхолдеров + {{COUNTRY_STATS_JSON}} добавлены в build_html(). {{SUB_LINK}} отсутствует (удалён в US-W05), что подтверждено проверкой.
  - Таблица протоколов переиспользует существующие `.cyber-table` и `.table-responsive` классы, добавляя только `.protocol-table` для кастомизации ячеек.
---

## 2026-06-04 - US-W08
- **What was implemented:**
  - Большинство критериев US-W08 уже было выполнено предыдущими историями (US-W01: протокольные файлы в exporter/merge + удаление sorted(); US-W07: 8 счётчиков статистики + {{COUNTRY_STATS_JSON}} в build_html). Эта итерация закрыла последний пробел AC #3 — плейсхолдер `{{NODE_STATS_JSON}}`.
  - **merge.py build_html():** Добавлен консолидированный `node_stats` dict (total, max_speed, bs/chs/ru/vless/vmess/trojan/ss/hy2, countries) → `node_stats_json`, подставляется в новый плейсхолдер `{{NODE_STATS_JSON}}`. Единый источник правды для клиентских графиков.
  - **template.html:** Добавлен атрибут `data-node-stats='{{NODE_STATS_JSON}}'` в `#stats-data` div (рядом с существующими data-*-count и data-country-stats).
  - **main.js initStats():** Теперь предпочитает blob `data-node-stats` (через хелпер `attr(name, key)`), с fallback на отдельные data-атрибуты при отсутствии/повреждении JSON. Аналогично для country distribution (node.countries → data-country-stats) и speed summary (node.max_speed/total → data-атрибуты).
  - **Прочие AC уже выполнены:** UTF-8 + #profile-title/#profile-update-interval (merge_subscription_files), пустые файлы не ломают сборку (пустой unique_map → только заголовки; generate_subscription([]) → только заголовки), порядок по скорости через dict вместо sorted().
- **Files changed:** merge.py, config/web/template.html, config/web/main.js
- **Also committed (leftover from prior sessions):** config/web/style.css (US-W07 FAQ/stats/protocol CSS не был закоммичен в US-W07), prd.json (US-W07 passes:true флаг)
- **Verification:**
  - `python -m py_compile merge.py core/exporter.py core/models.py` — OK
  - `node --check config/web/main.js` — OK
  - Smoke-test build_html() (со stubbed CONFIG): нет остаточных `{{...}}` плейсхолдеров, `data-node-stats` парсится в валидный JSON с верными счётчиками, пустой stats={} не падает
- **Learnings for future iterations:**
  - `pydantic_settings` и `aiohttp`/`loguru` отсутствуют в локальном окружении — для smoke-теста merge.py нужно стабить `core.settings.CONFIG`, `core.logger.GHA`, `loguru.logger` через `sys.modules` перед `importlib.util.exec_module`.
  - **Gotcha:** AC использовал имя `{{NODE_STATS_JSON}}`, но прежняя реализация (US-W07) уже завезла `{{COUNTRY_STATS_JSON}}` + индивидуальные data-атрибуты. Решение: добавить `{{NODE_STATS_JSON}}` как консолидированный blob, сохранив обратную совместимость с data-атрибутами (JS читает blob с fallback). Не ломать существующий рабочий путь рендеринга.
  - **Gotcha:** `index.html` — это собираемый артефакт (tracked, но регенерируется CI). НЕ коммитить локально сгенерированный index.html: после ручного запуска build_html всегда `git checkout -- index.html`.
  - Паттерн «blob + fallback»: единый JSON-плейсхолдер как источник правды, индивидуальные data-атрибуты как резерв — даёт и чистый контракт (AC), и устойчивость рендеринга если blob повреждён.
---

## 2026-06-04 - US-W09
- **What was implemented:**
  - **.github/workflows/update.yml** «Sync & Deploy» step: расширен `git add` с 3 файлов (index.html, sub_all.txt, sub_bs.txt, sub_chs.txt) до всех 9 подписок — добавлены sub_ru.txt, sub_vless.txt, sub_vmess.txt, sub_trojan.txt, sub_ss.txt, sub_hy2.txt. Теперь протокольные/RU подписки публикуются вместе с сайтом (git push --force на ту же ветку = деплой-источник для Vercel/gh-pages).
  - **vercel.json:** rewrites для /sub/vless, /sub/vmess, /sub/trojan, /sub/ss, /sub/hy2 (+ /sub/ru) и headers (Content-Type text/plain, Cache-Control no-store, CORS *) уже были на месте из US-W01/US-W04 — проверены, дублировать не нужно.
- **Files changed:** .github/workflows/update.yml, prd.json, progress.txt
- **Verification:**
  - `python -c "import json; json.load(open('vercel.json'))"` — OK
  - `python -c "import yaml; yaml.safe_load(open('.github/workflows/update.yml', encoding='utf-8'))"` — OK
- **Learnings for future iterations:**
  - merge.py пишет ВСЕ 9 подписок в корень репо: sub_all/bs/chs/ru/vless/vmess/trojan/ss/hy2.txt (см. merge_subscription_files вызовы ~строки 264-305). Любая новая подписка должна быть добавлена в `git add` шага «Sync & Deploy», иначе она генерируется но не публикуется.
  - **Gotcha:** прежняя заметка про «cp1251 encoding» update.yml неверна — файл валидный UTF-8 и грузится yaml.safe_load без ошибок (нужно явно указать `encoding='utf-8'` при open на Windows, где дефолтная локаль cp1251).
  - Vercel header-правило `/(.*sub.*)` покрывает и source-пути `/sub/<proto>`, и destination `sub_<proto>.txt` — отдельные header-блоки для каждого протокола не нужны.
  - Деплой = `git push --force` в той же ветке (не отдельная gh-pages orphan-ветка); add всех артефактов в одном коммите.
---
