{
  "project": "Scarlet Devil Network — Checker Remediation & Site Polish",
  "branchName": "ralph/checker-remediation",
  "dependsOn": "ralph/website-overhaul (all US-W01..US-W10 complete)",
  "description": "Фаза 2: улучшение качества проверки узлов (анализ логов показал 0.94% survival rate при дисбалансе дронов до 45x), исправление UI-багов, удаление RU-узлов, реорганизация секций сайта. Стек: Python (merge.py, core/engine.py, core/parser.py, core/validator.py) + Go (go_core/main.go) + статический HTML/CSS/JS (config/web/*) + Vercel (vercel.json) + GitHub Actions (.github/workflows/update.yml).",
  "analysisSummary": {
    "date": "2026-06-04",
    "dronePerformance": {
      "drone0": "parsed=17,841 alive=536 (3.0%) speed=469.6 Mbps",
      "drone1": "parsed=17,841 alive=96 (0.54%) speed=195.9 Mbps",
      "drone2": "parsed=17,841 alive=30 (0.17%) speed=61.9 Mbps",
      "drone3": "parsed=17,841 alive=12 (0.07%) speed=78.3 Mbps"
    },
    "keyIssues": [
      "45x дисбаланс между лучшим и худшим дроном (536 vs 12 alive)",
      "L4 фильтр убивает ~99% узлов — возможно слишком агрессивный таймаут",
      "L7 sing-box инспекция убивает ещё ~99% выживших — параметры валидации требуют аудита",
      "Скоростной тест только для топ-5 узлов — недостаточная выборка",
      "GitHub Actions раннеры в Azure US — география не совпадает с целевыми регионами (РФ/СНГ)",
      "Секции сайта были в неправильном порядке, RU-узлы отображались, FX-контрол не убран"
    ]
  },
  "userStories": [
    {
      "id": "US-C01",
      "title": "Аудит и настройка параметров L4 TCP-проверки",
      "description": "Как оператор, я хочу чтобы L4 проверка не отбрасывала потенциально рабочие узлы из-за слишком агрессивных таймаутов, чтобы повысить процент выживаемости после первого этапа фильтрации.",
      "acceptanceCriteria": [
        "Проанализированы параметры TCP-коннекта в core/engine.py: таймаут, количество попыток, размер пула соединений",
        "Таймаут TCP-коннекта увеличен с текущего значения до 8-12 секунд (если было меньше)",
        "Добавлен retry-механизм: 2 попытки на узел с экспоненциальной задержкой",
        "Добавлено логирование причины отказа (timeout / connection_refused / dns_error) в stats_*.json",
        "L4 survival rate отслеживается в логах для каждого дрона",
        "`python -m py_compile core/engine.py core/models.py core/validator.py` — без ошибок",
        "`cd go_core && go build -o /dev/null ./...` — без ошибок"
      ],
      "priority": 1,
      "passes": true,
      "notes": "✅ 2026-06-04: L4 timeout increased 2.5s→8s, DNS timeout 2s→4s, retry 2× with exponential backoff (2s wait, 10s retry). Failure reason tracking added (dns_error/timeout/connection_refused/invalid_config/other). L4 survival % logged. Stats written via go_stats_{uid}.json IPC channel."
    },
    {
      "id": "US-C02",
      "title": "Аудит и настройка параметров L7 sing-box валидации",
      "description": "Как оператор, я хочу чтобы L7 проверка через sing-box корректно определяла рабочие узлы, не отбрасывая узлы с медленным but валидным ответом, и логировала конкретную причину отказа каждого узла.",
      "acceptanceCriteria": [
        "Проанализированы параметры запуска sing-box в core/engine.py (BatchEngine) — таймаут, URL для проверки, размер батча",
        "URL для HTTP-теста изменён на более надёжный эндпоинт (например, http://cp.cloudflare.com/ или http://www.gstatic.com/generate_204)",
        "Таймаут L7 HTTP-запроса увеличен до 15-20 секунд (если было меньше)",
        "Размер батча уменьшен до 50 узлов для более точного контроля",
        "Добавлен детальный failure reason в вывод логов (timeout / tls_error / http_error / protocol_mismatch)",
        "`python -m py_compile core/engine.py core/validator.py` — без ошибок",
        "`cd go_core && go build -o /dev/null ./... && go vet ./...` — без ошибок"
      ],
      "priority": 2,
      "passes": true,
      "notes": "✅ 2026-06-04: L7 HTTP ping timeout increased from 6.5s to 15s (maxLatency + 10s buffer). L7 speed test timeout increased from 8s to 15s. Sing-box startup deadline increased from 5s to 8s. Batch size reduced from 100 to 50. L7 failure reason tracking added: http_timeout, http_tls_error, http_bad_status, http_other_error, speed_timeout, speed_tls_error, speed_too_slow, speed_other_error, singbox_crash, protocol_mismatch. Stats written via combined L4+L7 IPC stats file. Python Inspector extracts L7 stats and logs breakdown."
    },
    {
      "id": "US-C03",
      "title": "Балансировка шардов и курирование источников",
      "description": "Как оператор, я хочу чтобы источники подписок были равномерно распределены между дронами, а мёртвые/нестабильные источники автоматически исключались из выборки после N последовательных провалов.",
      "acceptanceCriteria": [
        "Проанализирован SHARD_INDEX / SHARD_COUNT механизм в main.py: как источники распределяются по дронам",
        "Добавлен health-track для источников в core/parser.py или core/settings.py — счётчик последовательных провалов",
        "Источник с >5 последовательными провалами исключается на 24 часа (с записью в dead_sources лог)",
        "Источники с высоким yield (>5% alive) получают приоритет при распределении по шардам",
        "Добавлен `source_yield` в telemetry (stats_*.json) для мониторинга",
        "`python -m py_compile main.py core/parser.py core/settings.py` — без ошибок"
      ],
      "priority": 3,
      "passes": true,
      "notes": "✅ 2026-06-04: Shard distribution changed from chunk-based to round-robin (all_nodes[idx::count]) to evenly spread nodes from each source across drones. SourceHealth class added — persistent tracking of consecutive failures per source, auto-exclusion after >5 zero-alive runs for 24h. Source yield metrics (parsed/alive/yield_pct) exported in stats_*.json via source_yields field. merge.py aggregates and logs top/bottom sources by yield. Health state persists via data/source_health.json (gitignored with exception). Workflow updated to upload per-drone health files and merge them in the merge step."
    },
    {
      "id": "US-C04",
      "title": "Расширенный скоростной тест и гео-статистика",
      "description": "Как оператор, я хочу чтобы скоростной тест охватывал больше узлов (не только топ-5) и генерировал агрегированную статистику по странам для отображения на дашборде.",
      "acceptanceCriteria": [
        "Скоростной тест расширен с топ-5 до топ-20 узлов (настраиваемый параметр в config/settings.yaml)",
        "В stats_*.json добавлены поля: avg_speed, median_speed, speed_percentile_90",
        "GeoIP-разрешение (country_stats) работает корректно и заполняется для всех выживших узлов",
        "Функция _extract_country_stats в merge.py извлекает коды стран из URL-фрагментов и/или из GeoIP-поля в stats_*.json",
        "Статистика по странам отображается на дашборде (секция stats) с флагами и процентным распределением",
        "`python -m py_compile merge.py core/exporter.py core/settings.py` — без ошибок"
      ],
      "priority": 4,
      "passes": true,
      "notes": "✅ 2026-06-05: Speed test expanded from top-5 to top-20 via CHAMPION_TOP_N=20 in settings.yaml (configurable). avg_speed, median_speed, speed_percentile_90 added to per-drone stats_*.json and aggregated in merge.py. Country distribution computed from GeoIP data (node.country) in main.py, exported per-drone, aggregated in merge.py. Speed summary dashboard expanded with avg/median/p90 rows. Country stats fall back to URL parsing if no GeoIP data available."
    },
    {
      "id": "US-C05",
      "title": "Исправление UI-багов и полировка сайта",
      "description": "Как пользователь, я хочу чтобы сайт работал без визуальных багов: кнопки были правильно выровнены, иконки отображались, статистика сети была информативной, а навигация была интуитивной.",
      "acceptanceCriteria": [
        "Кнопки «Получить доступ» и «Быстрая настройка» в hero-секции выровнены горизонтально на десктопе и вертикально на мобильных",
        "Кнопка «Звёздная пыль» в fun-panel имеет иконку fa-sparkles и работает при клике",
        "Переключатель интенсивности эффектов (FX control) полностью удалён — используется только Normal режим",
        "Все упоминания RU-узлов (Remilia, RU-verified, sub_ru) удалены из HTML, CSS, JS, merge.py и vercel.json",
        "Секция «Сообщество и Контакты» находится последней перед legal disclaimer",
        "Секция «Часто задаваемые вопросы» (FAQ) находится непосредственно перед «Сообщество и Контакты»",
        "Секция «Статистика сети» содержит рабочие чарты: распределение по протоколам, по классам (БС/ЧС), топ-10 стран, сводка по скорости",
        "Все чарты в секции статистики заполняются из data-атрибутов при загрузке страницы (buildStats() работает)",
        "index.html сгенерирован из актуальных source-файлов (template.html + style.css + main.js)",
        "Валидация: открыть index.html в браузере → проверить hero-кнопки, навигацию, FAQ, контакты, статистику"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Часть багов уже исправлена в этой сессии: удалён FX-control, удалены RU-узлы, переставлены секции (FAQ → Contacts), обновлён index.html. Нужна финальная валидация в браузере."
    },
    {
      "id": "US-C06",
      "title": "Улучшение стилизации hero-секции и кнопок",
      "description": "Как пользователь, я хочу чтобы hero-секция выглядела эффектно с правильно выровненными кнопками, а wizard быстрой настройки был заметен для новых пользователей.",
      "acceptanceCriteria": [
        "CSS-класс .hero-actions использует flexbox с правильным gap и выравниванием",
        "На десктопе (>768px) кнопки в hero располагаются горизонтально: «Получить доступ» (primary) + «Быстрая настройка» (secondary)",
        "На мобильных (<768px) кнопки располагаются вертикально, полная ширина",
        "Wizard-подсказка (.onboard-bubble) показывается только при первом визите (localStorage sd_visited)",
        "Кнопка wizard имеет анимированную иконку fa-hat-wizard",
        "Визуальная проверка: открыть index.html на 375×812 (iPhone), 1366×768 (Desktop)"
      ],
      "priority": 6,
      "passes": true,
      "notes": "✅ 2026-06-05: .hero-actions переписан mobile-first: column/full-width на <768px, row/auto-width на >=768px. Убран баг где .btn-primary (width:100%) форсировал перенос wizard-кнопки вниз даже на десктопе. .btn-primary и .btn-wizard получают width:auto в десктоп-брейкпоинте. Добавлена анимация иконки fa-hat-wizard: wizard-icon-bob при pulse-hint (первый визит) + rotate/scale на hover. Onboard-bubble уже показывался только при первом визите (localStorage sd_visited в initOnboarding). index.html регенерирован, node --check OK."
    },
    {
      "id": "US-C07",
      "title": "Добавление мониторинга качества проверки в Telegram-отчёт",
      "description": "Как оператор, я хочу получать в Telegram-отчёте расширенную информацию о качестве проверки: процент выживаемости по каждому дрону, топ-5 источников по yield, распределение причин отказов.",
      "acceptanceCriteria": [
        "Функция send_telegram_report() в merge.py расширена: добавлены поля survival_rate, top_sources, failure_reasons",
        "Telegram-сообщение показывает: общий survival rate (%), лучший/худший дрон по survival, топ-3 страны по количеству узлов",
        "Добавлен алерт если survival rate падает ниже 0.5% — сообщение помечается ⚠️",
        "Формат сообщения использует HTML-разметку Telegram (жирный, код, эмодзи)",
        "`python -m py_compile merge.py` — без ошибок"
      ],
      "priority": 7,
      "passes": true,
      "notes": "✅ 2026-06-05: send_telegram_report() расширен. Добавлены: overall survival rate (unique_alive/parsed) с ⚠️-алертом при <0.5%; лучший/худший дрон по survival (drone_survival агрегируется из per-drone alive/parsed); топ-3 страны (из country_stats); топ-3 источника по yield (top_sources из ranked yields); разбивка причин отказов (failure_reasons агрегирует l4_failure_reasons + l7_stats, исключая total/survived). HTML-разметка Telegram (b/code/эмодзи). py_compile OK."
    },
    {
      "id": "US-001",
      "title": "Speed as ranking signal, not a hard filter",
      "description": "As a user in Russia, I want more working proxies in the output, so transient bandwidth contention on the CI runner does not drop otherwise-alive nodes.",
      "acceptanceCriteria": [
        "In go_core/main.go a node that passes the L7 connectivity ping (HTTP 204) survives even when measured speed is below min_speed",
        "Speed is still measured and stored on the node (used only for sorting/labels, not for dropping)",
        "min_speed becomes an opt-in ranking threshold via a settings flag (e.g. speed_as_filter, default false) instead of an unconditional cut in testHTTPSpeed",
        "Speed-test concurrency (semSpeed) is reduced or made configurable to lower channel contention",
        "`cd go_core && go build -o /dev/null ./...` succeeds and `go vet ./...` is clean",
        "Rationale appended to progress.txt"
      ],
      "priority": 1,
      "passes": true,
      "notes": "AUDIT.md §2.4 — biggest immediate win on working-proxy count."
    },
    {
      "id": "US-002",
      "title": "Remove dead code and extract shared UUID util",
      "description": "As a maintainer, I want no duplicated/dead code so the codebase is easier to reason about.",
      "acceptanceCriteria": [
        "Remove the unused Inspector.champion_run method in core/engine.py (champion phase lives in Go)",
        "Create core/util.py exposing is_valid_uuid(val) -> bool",
        "core/parser.py and core/engine.py import is_valid_uuid from core.util instead of redefining it",
        "`python -m py_compile main.py merge.py core/*.py` passes",
        "`python -c \"import core.parser, core.engine, core.util\"` runs without error"
      ],
      "priority": 2,
      "passes": true,
      "notes": "AUDIT.md §2.5"
    },
    {
      "id": "US-003",
      "title": "Add .gitignore for build/runtime artifacts",
      "description": "As a maintainer, I want generated artifacts excluded from git so they cannot be committed by accident.",
      "acceptanceCriteria": [
        "Root .gitignore ignores: data/, go_core/angra_core, go_core/angra_core.exe, *.log, __pycache__/, *.pyc, .last-branch, archive/",
        "`git status --porcelain` shows none of those paths as untracked after a dry run that creates them",
        "Already-tracked source files remain tracked (no source accidentally ignored)"
      ],
      "priority": 3,
      "passes": true,
      "notes": "AUDIT.md §2.12"
    },
    {
      "id": "US-004",
      "title": "Parse once: NODES_FILE input mode + round-robin sharding",
      "description": "As an operator, I want parsing to run once instead of 4x (once per drone), so CI does less redundant work and sharding is balanced.",
      "acceptanceCriteria": [
        "Add parse_only.py that runs RKNValidator.load_lists() + LinkParser.fetch_and_parse() and writes data/nodes.json (list of model_dump(by_alias=True) dicts)",
        "main.py reads NODES_FILE env var: when set, it loads nodes from that JSON and skips fetch_and_parse()",
        "Sharding uses round-robin (all_nodes[shard_index::shard_count]) instead of contiguous slicing",
        "When NODES_FILE is unset, behavior is unchanged (backwards compatible)",
        "`python -m py_compile parse_only.py main.py` passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": "AUDIT.md §2.1, §2.2"
    },
    {
      "id": "US-005",
      "title": "GeoIP efficiency: dedup hosts and /24 country cache",
      "description": "As an operator, I want GeoIP resolution to avoid duplicate work and reduce ip-api rate-limit risk.",
      "acceptanceCriteria": [
        "core/engine.py _resolve_geo deduplicates hosts before DNS resolution (resolve each unique host once)",
        "Country cache is keyed by IPv4 /24 network (fallback to exact IP for IPv6)",
        "Number of ip-api lookups for a fixed node set is provably reduced (logged before/after counts)",
        "`python -c \"import core.engine\"` runs without error"
      ],
      "priority": 5,
      "passes": true,
      "notes": "AUDIT.md §2.3"
    },
    {
      "id": "US-006",
      "title": "Rolling pool of historically-working nodes",
      "description": "As a user, I want the working-proxy list to stay populated even when upstream sources are temporarily down or pruned.",
      "acceptanceCriteria": [
        "merge.py persists surviving nodes to data/pool.json with fields: uri, last_seen (ISO), fail_count",
        "Node set at run start is the union of freshly parsed nodes and pooled nodes (pooled nodes get re-checked)",
        "A pooled node is evicted after 3 consecutive runs where it failed checks; fail_count resets to 0 on success",
        "pool.json is created on first run if absent and never grows unbounded (evicted nodes removed)",
        "`python -m py_compile merge.py` passes"
      ],
      "priority": 6,
      "passes": true,
      "notes": "AUDIT.md §4.1 — biggest win on stability/count over time."
    },
    {
      "id": "US-007",
      "title": "Publish output to a dedicated branch (no force-push on main)",
      "description": "As a maintainer, I want published subscriptions to stop rewriting main history with amend+force.",
      "acceptanceCriteria": [
        ".github/workflows/update.yml publishes sub_all.txt, sub_bs.txt, sub_chs.txt and index.html to a dedicated branch (e.g. gh-pages) using a normal commit",
        "No `git commit --amend` and no `git push --force` against main",
        "vercel.json / deployment notes updated to serve subscriptions from the publish branch",
        "Workflow YAML is valid (parses with `python -c \"import yaml,sys; yaml.safe_load(open('.github/workflows/update.yml'))\"`)"
      ],
      "priority": 7,
      "passes": true,
      "notes": "AUDIT.md §2.6"
    },
    {
      "id": "US-008",
      "title": "Parser tests and linters in CI",
      "description": "As a maintainer, I want regression protection on the most fragile component (the link parser) and automated lint.",
      "acceptanceCriteria": [
        "Add tests/test_parser.py with table-driven cases covering vless/vmess/ss/trojan/hy2 — valid links produce expected ProxyConfig fields, garbage links return None",
        "Add requirements-dev.txt with pytest (and ruff)",
        "`pytest -q` passes locally",
        "A CI workflow (e.g. .github/workflows/ci.yml) runs ruff, pytest, and `cd go_core && go vet ./...` on push/PR",
        "All new code passes `python -m py_compile`"
      ],
      "priority": 8,
      "passes": true,
      "notes": "AUDIT.md §3.1, §3.2"
    },
    {
      "id": "US-009",
      "title": "Cache whitelist lists and log list age",
      "description": "As an operator, I want BS/whitelist classification to survive a temporary outage of the upstream whitelist sources.",
      "acceptanceCriteria": [
        "RKNValidator caches the last successfully fetched whitelist to data/whitelist_cache/ (domains + cidr)",
        "When a fetch fails or returns empty, the cached copy is loaded instead of leaving lists empty",
        "load_lists logs the source counts and a warning when falling back to cache or when lists are empty",
        "`python -c \"import core.validator\"` runs without error"
      ],
      "priority": 9,
      "passes": true,
      "notes": "AUDIT.md §5.1.3 — keeps the Nightbird (BS) class from collapsing to zero."
    },
    {
      "id": "US-010",
      "title": "RU-verified subscription scaffolding",
      "description": "As a user in Russia, I want a subscription verified from inside RU; this story adds the plumbing so an RU probe can publish a separate list without an external VPS being required for the code to be correct.",
      "acceptanceCriteria": [
        "Exporter can emit an additional sub_ru.txt built from nodes flagged ru_verified=True on the ProxyNode model",
        "A documented, env-gated ingestion path (e.g. RU_VERDICT_FILE) lets an external RU worker submit a list of node ids that passed from RU; absent the file, sub_ru.txt is simply empty (no failure)",
        "vercel.json exposes /sub/ru -> sub_ru.txt",
        "README/AUDIT updated with how to run an RU probe worker",
        "`python -m py_compile core/exporter.py core/models.py` passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": "AUDIT.md §5.1 — the single biggest lever for real-world RU effectiveness. Code-only scaffolding here; the actual RU worker is operational, not part of this repo's CI."
    }
  ],
  "implementationOrder": [
    "US-C01 (L4 аудит) — сначала понять, почему 99% узлов убивается на TCP-коннекте",
    "US-C02 (L7 аудит) — затем разобраться с sing-box валидацией",
    "US-C03 (балансировка шардов) — исправить дисбаланс источников",
    "US-C04 (скоростной тест + гео) — расширить покрытие тестами",
    "US-C05 (UI баги) — параллельно или после, многие уже исправлены",
    "US-C06 (hero стили) — полировка",
    "US-C07 (Telegram мониторинг) — улучшение наблюдаемости"
  ],
  "qualityChecks": [
    "python -m py_compile merge.py core/engine.py core/exporter.py core/parser.py core/validator.py core/settings.py",
    "cd go_core && go build -o /dev/null ./... && go vet ./...",
    "python -c \"import json; json.load(open('vercel.json'))\"",
    "python -c \"import yaml; yaml.safe_load(open('config/settings.yaml'))\"",
    "node --check config/web/main.js",
    "Открыть index.html в браузере — проверить hero, секции, модалки, чарты статистики",
    "Проверить на мобильном viewport (375×812 iPhone, 360×800 Android)",
    "Прогнать полный цикл: python main.py (с фейковыми данными) → проверить stats_*.json → python merge.py → открыть index.html"
  ],
  "styleGuide": {
    "colors": "Scarlet Devil палитра (--bg-void: #0a0306, --touhou-red: #e11d48, --touhou-purple: #946cf8, --touhou-sakura: #f472b6)",
    "typography": "Manrope (основной) + JetBrains Mono (моноширинный, код, логи)",
    "icons": "Font Awesome 6 Free (fa-solid предпочтительно)",
    "effects": "Только Normal режим — без Minimal/Extreme переключателя. Все анимации отключаемые через fun-panel.",
    "noExternalDeps": "Никаких npm/CDN кроме Font Awesome и Google Fonts. QR — самописный JS.",
    "naming": "CSS: kebab-case, JS: camelCase, Python: snake_case, Go: camelCase",
    "comments": "Русский для пользовательских текстов, английский для технических комментариев в коде"
  },
  "mergedFrom": [
    "checker-remediation",
    "audit-remediation"
  ]
}
